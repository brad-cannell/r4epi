# File paths

<!-- 
Exporting data for others (csv)
Exporting data for use in R (Rds)
We may also want to briefly touch on the difference between memory and disk.
-->

By default, when we create a data frame (or any object), it is created into something called the global environment. 

Use plain text files. They tend to just work.